@model IEnumerable<designhub.Models.File>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.DateCreated)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.FilePath)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.FileGroup)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateCreated)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FilePath)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FileGroup.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FileID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FileID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FileID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
